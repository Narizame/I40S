(defwidget clock []
    (label :class "clock" :text {formattime(EWW_TIME, "%H
%M")})
)

; Функция дефинираща големия часовник на работния плот:
(defwidget big_clock []
    (box :class "big_clock_box" :orientation "v" :space-evenly false
        (label :class "big_clock_label" :text {formattime(EWW_TIME, "%H:%M")})
    )
)